# Markdown-editor

Simple markdown editor using [React markdown](https://www.npmjs.com/package/react-markdown).

Script to run local dev server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
```
build using vite 
